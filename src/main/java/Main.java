import java.util.ArrayList;

public class Main {

    public static void main(String[] args) {
     /*   int numeberOfCars = 3;
        Road r1 = new Road(new Coordinates(45.3, 56.32), new Coordinates(46.3, 57.32), 43, 0.4);
        ArrayList<Vehicle> data = new ArrayList<Vehicle>();
        for (int i = 0; i < numeberOfCars; i++) {
            int prodYear = (int) (1996 + Math.random() * 10);
            double weight = 50 + Math.random() * 60;
            double speed = 90 + Math.random() * 60;
            double fuel = 10 + Math.random() * 50;
            double loadWeight = 50 + Math.random() * 60;
            data.add(new Truck(prodYear, weight, speed, fuel, loadWeight, r1));
            data.add(new Bus(prodYear, weight, speed, fuel, (int) loadWeight, r1));
        }
        data.sort((o1, o2) -> o1.compareDistance(o1, o2, 60));
        Road road1 = new Road(new Coordinates(45.3, 56.32), new Coordinates(46.3, 57.32), 43, 0.4);
        VehicleActivity vehicleActivity = new VehicleActivity(road1);

     ArrayList theMost =  vehicleActivity.getTheThreeMostFuelUsage(data, road1.getDistance());
*/

    }

}
